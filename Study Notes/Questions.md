1. Usually the private IP's network address is `192.168.0.0/24`, this means we can connect 254 devices with to this network. What happens if we connect more than 254 devices?
2. If we have `2n` devices and we have a switch every 2 devices (that mean we have `n` switches) will they connect to same interface on router or different interfaces?
3. Ask ChatGPT to explain *Route53* and the things associated to it like CNAME etc.
4. What is `cachedThreadPool` in Java, main question is when will java use thread from the cache rather creating new threads.